Патч (замена файлов) для ozon-tg-bot1

Что исправлено:
1) Чаты: chat_list переведён на /v2/chat/list + корректный filter + нормализация ответа (убирает 400 Bad Request).
2) Отзывы/Вопросы: более читабельный список и карточка (как «старый» UX), возвращён вывод названия товара.
3) Подпись опубликованного ответа: в карточках показывается «Ответ продавца на Ozon» + дата (если есть).
4) Названия товаров: batch-дозагрузка через /v3/product/info/list (sku/product_id/offer_id) с fallback.

Как применить:
- Распакуй ZIP в корень репозитория с заменой файлов.
- Пересобери и задеплой.
